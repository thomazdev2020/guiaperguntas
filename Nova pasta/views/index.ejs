<%- include ('./partials/header.ejs') %>

    <body>
        <%- include ('./partials/navebar.ejs') %>
             
            <div class="container">
                 
                 <h2 class="p-3">Fazer Perguntas <span><a href="/perguntar" class="btn btn-primary ml-2">Aqui!</a></span></h2>
                 

                <% Pergunta.forEach(Pergunta => { %>
                <div class="card mt-3">
                    <div class="card-body">
            <h3><%= Pergunta.titulo %></h3>
                    </div>
                    <div class="card-footer">
                     <a href="/pergunta/<%= Pergunta.id %>">   <button  class=" btn btn-primary mt-3">Responder</button></a>
                    </div>
                </div>
            <% }) %>
            </div>
    </body>
    <%- include ('./partials/footer.ejs') %>